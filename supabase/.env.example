# Only server secrets. Never copy these values into frontend assets.
DNI_HMAC_SECRET=replace-with-at-least-32-random-characters
ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200
# Optional: image descriptions. Choose an available vision model in your account.
GEMINI_API_KEY=
GEMINI_VISION_MODEL=
